<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Structured JSON logs to stdout. MDC carries the delivery id being processed. -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>deliveryId</includeMdcKeyName>
            <customFields>{"service":"eventrelay-dispatcher"}</customFields>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="JSON"/>
    </root>
    <logger name="com.eventrelay" level="DEBUG"/>
</configuration>
